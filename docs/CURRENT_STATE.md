# AGR Current State

Updated: 2026-09-22

Active target: Android Framework Runtime Traversal Dispatch / Surface Draw Consumer Phase 1. Lifecycle: `IMPLEMENTED`. Integration base: `main @ 6e47ce9ab6da32483f049731ac04f2a8356a4b67`. Formal Runtime baseline and last known good remain `3e3db84a53ad0957e9217f804b6f718a942b9a11` / tree `1af351ebd1987d1d19b8bee83f9a7a8aa5071ffa`.

Normal Activity start still returns at `handoff.viewroot_traversal` with the traversal scheduled and `traversal_count` 0. API19 `ViewRootImpl.scheduleTraversals` posts `Choreographer.CALLBACK_TRAVERSAL` and does not call `doTraversal`. `doCallbacks` extracts due callbacks before running them, so a Surface-acquisition reschedule waits for the next frame. AGR's public consumer is one host frame: `agr_dex_game_choreographer_frame` calls `agr_viewroot_choreographer_frame` once. The first frame may acquire the Surface and reschedule; the second frame keeps that Surface and enters `performDraw`. `start_activity` does not call `do_traversal`. The closed explicit first traversal still ends at `handoff.viewroot_surface_ready` with `draw_count` 0.

Discovery run `35666933096` launched unchanged Frozen Bubble with UIKit `CADisplayLink` and no harness `do_traversal`. The report is `viewroot_draw_entered`: vsync 2, traversal counts 0 then 1 then 2, Surface generation 1, `draw_count` 1. The job then failed in the contract checker because NSJSON encoded the synthetic `passed` flag as `1`. That is a harness defect. `performDraw` of the host Decor is not a first frame. Discovery run `35668200026` reached the same draw entry, but `FrozenBubble.onCreate` calls `setContentView(int)` and the int overload was bound to `setContentView(View, LayoutParams)`, so no content child was installed. `Activity.setContentView(int)` now inflates the layout resource. Discovery run `35670955623` on `9508063` confirms the normal UIKit path: resume has `content_view_installed`, `content_child_count` 1, child id `0x7f060000`, layout -1,-1, `traversal_count` 0 and still scheduled; two host frames then reach `viewroot_draw_entered` with traversal counts 1 then 2, Surface generation 1, `draw_count` 1, and `harness_called_do_traversal` false. That draw is the host Decor. It is not an observable first frame. The protected explicit first traversal in the same run still ends at `handoff.viewroot_surface_ready` with `draw_count` 0. The SurfaceView contract now creates a distinct child Surface on the draw-pass pre-draw hook. The Linux host contract passes registration, one surfaceCreated, one surfaceChanged, identity, generation, hidden and zero-size rejection, removed callbacks, and allocation failure. A local host probe of unchanged Frozen Bubble registers GameView as the callback during onCreate. Frame 1 does not create the child Surface. Frame 2 reports generation 1, 320x480, format 4, created_count 1, changed_count 1, and an identity distinct from the root Surface. Discovery run `35683118511` on `68ed706` confirms the same UIKit path at 1206x2622: frame 1 created_count 0, frame 2 created_count 1 and changed_count 1, content identity distinct from the root Surface, classification `viewroot_draw_entered`, and `harness_called_do_traversal` false. The next stop was `GameThread.scaleFrom` with `NullPointerException`. That was not a first frame. `BitmapFactory.decodeResource(Resources, int, Options)` initially returned size from the APK PNG, JPEG, or GIF path/dimensions only. The `scaleFrom` null field was `GameThread.cleanUp`, reached because `GameView.<init>` pc 35 invoke-virtual method_idx 252 resolved `java.lang.Thread.start` at local vtable index 0 and `GameThread.vtable[0]` was `cleanUp`. pc 30 method_idx 247 `setRunning` already selected `GameThread.setRunning`. Framework and guest classes now share one flattened vtable: `Thread.start` is slot 10 and the same slot on `GameThread` is `java.lang.Thread.start`. `java.lang.Thread.start` now returns after it launches a joinable host worker. That worker has its own `DxExecutionContext`: frame, pending exception, and instruction budget are not the caller's. A local unchanged Frozen Bubble probe reaches launch stage RESUMED (`start_activity` 0, stage 9). Three host frames then report `surfaceCreated` 1, `surfaceChanged` 1, and a content Surface distinct from the root Surface. `GameThread.run` is entered on execution context 1 while the caller remains context 0. `GameThread.run` pc 36 is invoke-interface method_idx 71 `SurfaceHolder.lockCanvas(Rect)` with a null rect. This DEX has no no-arg `lockCanvas` method id. `unlockCanvasAndPost` is method_idx 72 at pc 62 and on the exception path at pc 86. `lockCanvas(Rect)` now returns one guest `Canvas` bound to the SurfaceView child content Surface for the current generation. The host mutex covers only the locked-flag update. A second lock from the same execution context returns null. Another execution context does not obtain a second writer. `unlockCanvasAndPost` of that Canvas releases the lock and increments `post_count`. `BitmapFactory.decodeResource` now decodes PNG, JPEG, and GIF through existing `agr_bitmap` (KitKat SkImageDecoder) and retains host pixel backing keyed by guest Bitmap identity. `Canvas.drawBitmap(Bitmap,float,float,Paint)` with null Paint blits those pixels onto the locked content Surface with clipped SRC_OVER. A local unchanged Frozen Bubble probe after three host frames and 300 ms reports surfaceCreated 1, surfaceChanged 1, format 4, 320x480, row bytes 1280, generation 1, content Surface distinct from the root, lock_count 6, unlock_count 6, post_count 6, draw_bitmap_count 12, lock owner execution context 1, locked generation 1, post generation 1, pixel_change_count 1006080, and post buffer hash `1cf638b96fa0056` distinct from the empty-buffer hash `4f2d9f141eeda325`. LOCK_ACQUIRED, BITMAP_PIXELS_READY, DRAW_MUTATED, POSTED, and FIRST_CONTENT_FRAME hold. Visual completeness is PARTIAL. The harness did not call lockCanvas, drawBitmap, or unlockCanvasAndPost. The first drawn resource is `res/drawable/background.jpg` (JPEG 640x480). DEX evidence: `Sprite.drawImage` invoke-virtual method_idx 31 shorty VLFFL with Paint null. On the Linux 320x480 probe `mDisplayScale` is 1, so `scaleFrom` keeps the decoded bitmap and does not call `Bitmap.createScaledBitmap`. Focused Simulator discovery `35707262789` on `53607c1` launched the normal UIKit path at 1206x2622 with `harness_called_do_traversal` false and `harness_called_render_api` false. After the observation window the content Surface was locked and posted 3 times, `canvas_draw_bitmap_count` was 0, `canvas_pixel_change_count` was 0, and the buffer hash was unchanged. The root-exec method trace is `GameThread.scaleFrom` calling `Bitmap.getWidth` and `Bitmap.getHeight`. DEX `scaleFrom` pc 57 is invoke-static `Bitmap.createScaledBitmap` shorty LLIIZ with filter true, and the unresolved invoke returns null, so `BmpWrap.bmp` stays null and `Sprite.drawImage` calls `drawBitmap` with a null Bitmap. `Bitmap.createScaledBitmap` now returns scaled host pixels. Discovery `35709317186` on `e4cbb2b` then reported lock 3, draw_bitmap_count 4, pixel_change_count 6900732, post 2, and `res/drawable/background.jpg` at 3496x2622, but `canvas_buffer_hash_before` equaled `canvas_buffer_hash_after` because each later lock replaced the pre-draw hash. That hash is now kept from the first lock of the buffer. Discovery `35710347602` on `f7a43a4` drew once (`pixel_change_count` 3162132) and closed while the Canvas was still locked, so `post_count` was 0. The report now waits for the first posted changed buffer or 4 seconds. The discovery contract fails unless lock, drawBitmap, pixel change, post, and a changed hash are all present. Discovery `35711118165` on `4f229c5b91653f6ca0f1d95bc23de40a29e0998a` meets that gate: `harness_called_do_traversal` false, `harness_called_render_api` false, surfaceCreated 1, surfaceChanged 1, lock_count 1, draw_bitmap_count 2, pixel_change_count 3450366, post_count 1, hash_before `12442867522110196069` differs from hash_after `3039551882826320397`, bitmap `res/drawable/background.jpg` 3496x2622. FIRST_CONTENT_FRAME is SIMULATOR_CONFIRMED. Visual completeness stays PARTIAL. `java.lang.Character.forDigit` is the API19 static method on the game VM: radix 2..36 and a digit in range return `'0'+digit` or `'a'-10+digit`, otherwise 0. A local unchanged Frozen Bubble probe consumed it: execution context 1, `GameThread.drawLevelNumber` pc 18, opcode `0x71` invoke-static, method_idx 82, shorty `CII`, arguments digit 1 and radix 10, return 49. The next guest invoke is `BubbleFont.getCharIndex` with that char. Character.forDigit is REAL_GAME_CONFIRMED. `Canvas.save(int)`, `Canvas.clipRect(float,float,float,float,Region.Op)`, and `Canvas.restore` keep a bounded clip stack on the locked content Surface and feed the current clip to the existing `agr_bitmap_draw`. A local unchanged Frozen Bubble probe consumed `Sprite.drawImageClipped` on execution context 1: save flag 2 returned save count 1 and stack depth 2, `clipRect` `Region.Op.REPLACE` coords 25,433,35,455 changed the clip from 0,0,320,480 to 25,433,35,455 and returned true, `drawBitmap` wrote 220 pixels inside that clip, and `restore` returned the clip to 0,0,320,480. Canvas save/clip/restore is REAL_GAME_CONFIRMED. `java.util.Vector` core is REAL_GAME_CONFIRMED for `Vector()`, `size`, `addElement`, `removeElement`, and `elementAt`. A local unchanged Frozen Bubble probe shows `GameScreen.paint` size 1, `elementAt(0)` returning the PenguinSprite stored by `addSprite`, then `PenguinSprite.paint`. `Activity.requestWindowFeature(int)` now follows API19 `PhoneWindow.requestFeature`. `FEATURE_NO_TITLE` (1) before content returns true and stores bit `1<<1` on the Activity's `Landroid/view/Window;`, so default features 65 become 67. The same window is the `setContentView` window, and the bit remains after content is installed. A later request throws `AndroidRuntimeException` and does not change the bits. `FEATURE_NO_TITLE` dominates `FEATURE_ACTION_BAR`. `FEATURE_CUSTOM_TITLE` combined with `FEATURE_NO_TITLE` throws. Guest-visible `FEATURE_NO_TITLE` state is preserved; the downstream visible effect is currently absorbed by the AGR host window shape. This was the earliest remaining unresolved public API on execution context 0. It was not a proven blocker: the unchanged APK already reached the game thread and `PenguinSprite.paint`. A post-fix local probe resolves `FrozenBubble.onCreate` pc 8 opcode `0x6e` method_idx 185 shorty `ZI` argument 1, returns 1, and still reaches `GameScreen.paint` → `Vector.elementAt` → `PenguinSprite.paint` with lock 6, post 6, and draw_bitmap 24. Unresolved invoke telemetry is owned by each DxExecutionContext. Context-registry enumeration takes the existing VM `shared_mu` only long enough to copy a published context pointer and that context's atomic trace. `Activity.getIntent()` returns the Intent already stored in the Activity `_intent` field. On the unchanged APK, `FrozenBubble.onCreate` pc 11 opcode `0x6e` method_idx 171 returns activity `94599168163488`, field `94599168165184`, and that same launch Intent, with no exception. The following local probe prints `UNRESOLVED_CONTEXT exec=0 count=14 dropped=0` and `UNRESOLVED_CONTEXT exec=1 count=0 dropped=0`. Execution context 1 recorded zero unresolved invokes during this observation window. The first unresolved public invoke directly observed on execution context 0 is now `FrozenBubble.onCreate` pc 17 `Intent.getExtras` shorty `L`. That is the next candidate encounter. The two traces are not a cross-thread program order. `java.lang.Thread` remains a HOST-DEX execution context and is not a guest pthread; one guest pthread remains one Darwin pthread. Guest-visible surface format stays PixelFormat.RGB_565 (4). Host content backing is little-endian RGBA (R,G,B,A) at 4 bytes per pixel.

## Physical device runtime

FIRST_CONTENT_FRAME stays SIMULATOR_CONFIRMED. Physical-device runtime confirmation is not yet confirmed. The iphoneos default entry still loads unchanged `org.jfedor.frozenbubble_8` and drives `agr_dex_game_choreographer_frame` from `CADisplayLink`. It does not call `do_traversal`, `lockCanvas`, `drawBitmap`, or `post`. The device path now opens a durable forensic channel before the APK is touched: `Documents/agr-physical-run.json`, append-only `Documents/agr-physical-trace.ndjson`, and the existing `Documents/agr-physical-runtime.json` summary. A dedicated pthread watchdog records 2s/5s/8s no-progress markers and does not kill the process. Repeating canvas lock, draw, and post append without a per-frame fsync. The first `THREAD_RUN_ENTER`, `SURFACE_CREATED`, `SURFACE_CHANGED`, `CANVAS_LOCK_ACQUIRED`, `DRAW_BITMAP_END`, `CANVAS_POST_END`, and `PENGUIN_SPRITE_PAINT_WITNESS` are durable checkpoints. Failure, runtime error, 8s no-progress, stall, and finalize events are durable on every occurrence. The watchdog try-locks the existing content-surface mutex and records `SNAPSHOT_UNAVAILABLE` when that mutex is busy. It does not take the mutex with a blocking lock and does not call guest code. `agr_physical_trace_finish` is the single termination reason written to the run file, the finalize trace, and `agr-physical-runtime.json`. Catchable fatal signals write `Documents/agr-physical-crash.bin`; `SIGKILL` cannot be caught. `SCREEN_PRESENTATION` is not claimed. `Intent.getExtras` remains the next execution-context-0 encounter and is not selected as a blocker. Physical device evidence on `e444be7` at 1080×2340 posted 119 times with `draw_bitmap_count` 0, `pixel_change_count` 0, and equal buffer hashes. `surface_created` 1 and `surface_changed` 1 did not prove the guest callbacks returned: a thrown callback returned -1 and the old `if (result)` test counted that as success. Callback counts now advance only for `CALLBACK_OK`. A throw keeps the exception pending, records `SURFACE_CALLBACK_THROW` with the exception class, and does not increment the count. Linux at the same 1080×2340 display and the same APK SHA draws, changes the buffer, reaches `PenguinSprite.paint`, and reports Vector size 1, so that device failure is not a host reproduction. Simulator run `35775149021` at UIScreen native bounds 1206×2622 also drew (lock 1, draw_bitmap 4, pixel_change 3450366, post 1, changed hash, callback counts 1/1, no callback exception) because `AGR_HOST_DISPLAY_*` never entered the Simulator process. The traversal-dispatch launch now forwards those variables with the `SIMCTL_CHILD_` prefix. The next device trace distinguishes callback throw, scale failure, and a `doDraw` return before a Canvas operation. Root execution context is the Activity `ACTIVITY_START_OK` context, not the first worker `EXEC_PUBLISHED`. A post with no draw classifies as `CONTENT_POSTED_WITHOUT_DRAW`. `PHYSICAL_DEVICE_RUNTIME` is `PENDING_REVALIDATION`. `SCREEN_PRESENTATION` is `NOT_YET`. The current-root recorder now resets at process launch: one `run_id` and one `process_launch_id` cover `agr-physical-run.json`, the trace, and the final runtime file. A previous in-progress run is archived as `ABRUPT_TERMINATION`, `NATIVE_SIGNAL_CRASH`, or `WATCHDOG_STALL`. The run file embeds the public environment, the Mach-O UUID and SHA-256 of the executable plus `libEGL` and `libGLESv2`, the observation window and stop reason, and CADisplayLink frame tokens. A bounded guest snapshot records the GameThread frame at `THREAD_RUN_ENTER`, the first lock, and the first post. Read-only witnesses may record `mRun`, `mImagesReady`, `doDraw`, `setSurfaceSize`, `resizeBitmaps`, `FrozenGame` construction, and `Vector.size` without changing guest results. Memory warnings, thermal changes, low-power changes, protected-data changes, and active/background transitions are trace events. Do not install the `e444be7` IPA or the `09b1bc` IPA.

## Merged First Traversal baseline

Formal baseline and last known good: `main @ 3e3db84a53ad0957e9217f804b6f718a942b9a11` / tree `1af351ebd1987d1d19b8bee83f9a7a8aa5071ffa`. First Traversal is `MERGED`. Closure run `35634763769` is `VALID_PASS`: linux-source-contract, focused Simulator, protected Runtime contracts, bounded Simulator smoke, protected Runtime and real-game regressions, and the arm64 iphoneos build all passed. Post-merge run `35662355600` passed on governance-only follow-up `79d47bc6578a36c7af6a10d5b5ec03122fdbd1da`.

The candidate commit's ledger still said `IMPLEMENTED` with `closure_tested_commit = null` because that commit is the exact tested tree. The closure artifact was produced after it. That ledger is not evidence that closure did not occur.

The prior `handoff.viewroot_traversal` is now consumed by a host-side API19 ViewRoot first traversal. Focused Simulator run `35631242737` executed a synthetic Activity and the unchanged Frozen Bubble APK through attachment, root measurement, WindowSession relayout, persistent Surface acquisition and layout. The synthetic contract also observed first-Surface rescheduling, the second traversal retaining the same backing identity, relayout rejection, Surface allocation failure and retry. The current terminal is `handoff.viewroot_surface_ready`; draw/Canvas/GLSurfaceView and gameplay remain outside this phase.

The authoritative source path is pinned `android-4.4.4_r2` `ViewRootImpl.doTraversal/performTraversals`, `View.measure/layout`, `IWindowSession.relayout`, and `Surface` validity. UIKit supplies display dimensions; the in-process WindowSession HLE owns Android frame/Surface policy. The source map and semantic differential for this phase are under `ci/governance`.

## Previous closed ViewRoot attach baseline

Prior ViewRoot attach lifecycle: `MERGED/STABLE`. Its closure-tested commit `a6256ba4e4f0d8124bc5a2348039b882f2c91dc8` / tree `9d0dedc1b6bcd92ad40a595b386ce798d5da3025` passed exact-target post-merge run `35598935478`.

## Previous target

Migrate the Android 4.4.4 ViewRoot attach cluster after WindowManager.addView: ViewRoot creation, root/parent assignment, initial traversal scheduling, WindowSession attachment, and attach completion. The phase ends at `handoff.viewroot_traversal`; performTraversals, relayout, Surface and drawing remain downstream.

## Earliest Evidenced Divergence

Focused Simulator workflow `35528665347` ran the unchanged Frozen Bubble APK and retained its VM for 2000 ms. The exact method and Framework traces ended at `coordinator.performResume.return`; no exception, method, instruction or Framework event followed. Pinned API19 `ActivityThread.handleResumeActivity` instead continues immediately with `Activity.getWindow`, DecorView acquisition, WindowManager attachment, visibility publication and idle-handler scheduling.

Classification: `ANDROID_SEMANTIC_BUG`. Semantic class: `PUBLIC_OBSERVABLE`. Causal status and evidence level: `REAL_GAME_CONFIRMED`.

## Implemented Candidate

`agr_viewroot_attach` is the formal API19 host-side ViewRoot owner. It owns a ViewRoot and AttachInfo object, Decor/parent relation, a pending first traversal and an in-process WindowSession attachment before emitting `handoff.viewroot_traversal`. The source map and semantic diff are recorded for `WindowManagerImpl.addView`, `WindowManagerGlobal.addView`, and `ViewRootImpl.setView/requestLayout/scheduleTraversals`.

The exact candidate passed focused contract, unchanged real-APK regression, Simulator, iphoneos and closure run `35595440089` as `VALID_PASS`. Post-merge run `35596315143` selected unrelated historical PVS1 evidence and remains INVALID. Corrected run `35598935478` bound the ViewRoot closure to the exact merged tree and passed identity, governance, JNI and focused ViewRoot gates.

## Protected Boundaries

- Original APK and DEX remain unchanged.
- This is host-side DEX/Framework HLE; GUEST-ARM native ownership is unchanged.
- Existing linker, pthread, EHABI, allocator, NativeActivity/Input and graphics contracts remain protected.
- No per-game Runtime behavior is permitted; the harness may identify Frozen Bubble and assert its expected observable trace.

## Closure Contract

Exact-commit closure and corrected post-merge validation are complete. ViewRoot attach is MERGED/STABLE for this contract. No Surface, relayout or rendering claim is made.
# Active integrated candidate: physical Bitmap decode/build parity

## New physical evidence and active first-visible-frame boundary

The newer exact iPhone launch `000168ac811f20380000fd326ab452e5` at `cdbaf341` / tree `06a1685d` used the unchanged Frozen Bubble APK (`57f4735297befc68c0a7aa6cd9e442ecd250b1b2b38104324a12b6c2d4e18569`). It passed Bitmap decode and scale, entered GameThread draw, changed 5,284,440 pixels, and completed one child-Surface post. The screen stayed black. The current run file was modified after the manifest was sealed, so the old full bundle has `HASH_MISMATCH`; its trace and runtime hashes match. Historical evidence is retained rather than relabeled as a valid manifest.

The API19 post follows `Surface.unlockCanvasAndPost` to `Surface::unlockAndPost` and `IGraphicBufferProducer::queueBuffer`; AGR stopped at a post counter and had no iOS consumer. The observer also destroyed the game during the next draw. This evidence supersedes the earlier active hypothesis that the first physical Bitmap scale still fails, while preserving the old NPE as history. The primary blocker is now the first missing completed-Surface consumer at the platform display boundary; early observer termination is a separate harness defect. The active branch remains **IMPLEMENTED / UNVERIFIED**, with no claim of Simulator screen or physical screen presentation for the new candidate. `ci/governance/semantic-diffs/framework-surface-post-display.json` records the source and evidence. The exact next validation is the focused host contract, same-candidate Simulator screen capture, iphoneos build, and physical iPhone observation.


The original desktop device evidence is now directly audited, not inferred from a report: legacy run `00012ea72cdb18b80000cdd96ab359b3` at commit `3dc5fb0`, APK SHA-256 `57f4735297befc68c0a7aa6cd9e442ecd250b1b2b38104324a12b6c2d4e18569`, records the first `scaleFrom` at seq 292, null source at `createScaledBitmap` seq 298, and `surfaceChanged` NPE at seq 304. The original APK DEX first decodes `res/drawable/background.jpg` into `mBackgroundOrig`, then passes that field to the first `scaleFrom`. The device build script omitted JPEG/GIF Skia decoders and codec objects; Linux and Simulator include them. This is the earliest source/build divergence explaining the device-only null Bitmap. The current candidate unifies iOS codec inputs and retains the passive object-chain probe and historical trace archive from `b6539f3`. This is an **IMPLEMENTED / UNVERIFIED** repair: the new device executable and physical first-content-frame have not been validated. Existing Linux/Simulator first-frame evidence applies only to its recorded commits. Device absence and GitHub write authentication currently prevent those external checks, not the local source/trace audit.
