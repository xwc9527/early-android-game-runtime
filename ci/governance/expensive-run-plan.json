{
  "schema_version": 1,
  "enabled": true,
  "question": "Can the test-only trajectory host on iOS Simulator 27.0 build, keep the exact original APK executing for 30 host frames, publish a bounded checkpoint with exact build/environment identity, and expose a working external Simulator HID client without game input injection?",
  "current_uncertainty": [
    "The host checkpoint adapter was committed at cd5d9a7 but existing Linux real-game contract fails on its earlier Runtime path before the dependent Simulator job can compile this test-only code. A standalone focused iOS 27.0 preflight is necessary to verify the host adapter and external HID tooling before canonical replay; no gameplay result is inferred from this run."
  ],
  "expected_outcome_a": {
    "result": "The Simulator builds and runs the current Runtime on iOS 27.0, checkpoint host_vsync advances, exact APK/build identity is present, and idb discovers the booted Simulator.",
    "excludes": [
      "a test-only host compile or launch failure as the immediate obstacle to trajectory replay",
      "an unavailable external HID client as the immediate obstacle to UIKit-originated input"
    ]
  },
  "expected_outcome_b": {
    "result": "Build, checkpoint, environment identity, or external HID tool availability fails with a bounded stage and retained artifact.",
    "excludes": [
      "claiming the iOS trajectory harness is usable before it is compiled and exercised",
      "treating any Android-vs-AGR game difference as valid before the reference trajectory is frozen"
    ]
  },
  "why_cheaper_test_is_insufficient": "Windows can validate the Python schema and scripts, but cannot compile the UIKit host or verify idb against the iOS 27.0 Simulator. The existing dependent Simulator workflow is skipped by a pre-existing Linux real-game contract failure unrelated to this test-only adapter.",
  "prior_expensive_runs_without_information_gain": 0
}
