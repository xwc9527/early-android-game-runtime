{
  "schema_version": 1,
  "enabled": true,
  "question": "Does unchanged Frozen Bubble on the normal UIKit CADisplayLink path lock the SurfaceView content Surface, draw a decoded Bitmap into it, and post a changed buffer without a harness render call?",
  "current_uncertainty": [
    "Discovery 35710347602 on f7a43a4 kept the empty pre-draw hash and drew background.jpg once (pixel_change_count 3162132) but closed the report during that lock, so post_count was 0 and hash_after was 0. The same question is rerun after the report waits for the first posted changed buffer or a 4 second bound."
  ],
  "expected_outcome_a": {
    "result": "harness_called_do_traversal false, harness_called_render_api false, surfaceCreated 1, surfaceChanged 1, content Surface distinct from the root, lock_count > 0, draw_bitmap_count > 0, post_count > 0, pixel_change_count > 0, and the posted buffer hash differs from the pre-draw hash",
    "excludes": [
      "the claim that the Linux content frame is host-only and does not occur under UIKit CADisplayLink"
    ]
  },
  "expected_outcome_b": {
    "result": "the UIKit path reaches the content Surface but lock, drawBitmap, or the posted hash does not show a pixel change",
    "excludes": [
      "treating the Linux probe as Simulator confirmation",
      "continuing to Character.forDigit before the content-frame divergence is fixed"
    ]
  },
  "why_cheaper_test_is_insufficient": "The Linux host contract and the local APK probe do not execute UIKit CADisplayLink, the iOS Simulator Skia/JPEG link, or the display-link observation window. Source inspection cannot show whether that host path posts a changed content buffer.",
  "prior_expensive_runs_without_information_gain": 0
}
