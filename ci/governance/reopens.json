{
  "schema_version": 1,
  "reopens": [
    {
      "module": "NativeActivityInput",
      "target": "Phase 3D Dalvik GC roots and object lifetime",
      "reason": "The guest JNI owner change remains in this branch. Phase 3D does not add a second reference table.",
      "evidence": "Phase 3A VALID_PASS a2e07eb37f645f39c9a968b78808e88cdc25c6e3. Guest class and string handles are Dalvik indirect refs."
    },
    {
      "module": "DEXRuntimeLifecycle",
      "target": "Phase 3D Dalvik GC roots and object lifetime",
      "reason": "game_dex_runner.c still publishes references through the VM indirect-ref tables that Phase 3D marks as roots.",
      "evidence": "Phase 3A VALID_PASS a2e07eb37f645f39c9a968b78808e88cdc25c6e3. Class mirrors use the VM global ref table."
    },
    {
      "module": "NativeActivityInput",
      "target": "Phase 3C Dalvik exceptions and native binding",
      "reason": "The Phase 3A guest JNI owner change remains in this branch. NativeActivityInput stays open for that indirect-ref owner; this phase does not add a second JNI table.",
      "evidence": "Phase 3A VALID_PASS a2e07eb37f645f39c9a968b78808e88cdc25c6e3. agr_guest_runtime.c class and string handles are Dalvik indirect refs."
    },
    {
      "module": "DEXRuntimeLifecycle",
      "target": "Phase 3C Dalvik exceptions and native binding",
      "reason": "game_dex_runner.c still publishes class and string values through the VM indirect-ref tables. The Phase 3C exception work uses that same owner.",
      "evidence": "Phase 3A VALID_PASS a2e07eb37f645f39c9a968b78808e88cdc25c6e3. agr_dex_game_class_ref returns the Class mirror global ref."
    },
    {
      "module": "NativeActivityInput",
      "target": "Phase 3A Dalvik JNI references",
      "reason": "Guest ARM JNI minted class handles at 0x64000000, string handles at 0x66000000, and the instance receiver 0x60001000. Those values bypass Dalvik indirect references. FindClass, NewStringUTF, and the native receiver now use the same global indirect-ref table as HOST-DEX.",
      "evidence": "API19 IndirectRefTable encodes kind in the low 2 bits and rejects a stale serial. agr_guest_runtime.c previously returned 0x64000000+index and passed 0x60001000 as the jobject. Tests/DexLoom/jni-ref-host.c shows a global ref kind of 2 and rejects a deleted handle."
    },
    {
      "module": "DEXRuntimeLifecycle",
      "target": "Phase 3A Dalvik JNI references",
      "reason": "game_dex_runner.c object_handle minted 0x67000000 synthetic ids. Native arguments now publish a Dalvik global indirect ref from the VM table.",
      "evidence": "The same IndirectRefTable owner used by dx_jni.c NewGlobalRef. A raw 0x67000000 value is not a Dalvik reference kind."
    },
    {
      "module": "DEXRuntimeLifecycle",
      "target": "API19 Surface post to iOS display endpoint",
      "reason": "The current exact physical trace proves Canvas.drawBitmap mutates a valid child Surface and unlockCanvasAndPost increments post_count, while the iOS window remains an unconnected black UIViewController. The published post has no consumer-owned immutable buffer. Reopening is limited to Surface post ownership and a read-only host-consumer handoff; Bitmap, guest drawing, and SurfaceHolder lock semantics stay unchanged.",
      "evidence": "Physical run 000168ac811f20380000fd326ab452e5 at cdbaf341, trace/runtime hashes match manifest, CANVAS_POST_END seq 855 then second-frame DRAW_BITMAP_BEGIN seq 875 and harness FINALIZE_BEGIN seq 878. API19 android_view_Surface.cpp nativeUnlockCanvasAndPost calls Surface::unlockAndPost; KitKat Surface.cpp then queues the buffer through IGraphicBufferProducer. AGR has no corresponding consumer handoff."
    },
    {
      "module": "NativeActivityInput",
      "target": "API19 Surface post to iOS display endpoint",
      "reason": "The physical observation harness ends and destroys the game after the first content post, during a second guest draw. The iOS endpoint must consume posted Surface buffers without executing guest code, and observation must not terminate an active producer.",
      "evidence": "Same physical trace seq 855-879; App/main.m pollPhysicalReport finishes on first post and finishPhysicalReport destroys gPhysicalGame. The physical UIKit root is a black UIViewController with no posted-Surface consumer."
    },
    {
      "module": "NativeActivityInput",
      "target": "DEX Parser Compatibility Phase 1",
      "reason": "App/main.m contains the generic APK smoke harness whose DEX-stage classifier is the evidenced primary blocker; the change does not alter NativeActivity/Input production semantics.",
      "evidence": "focused runs 35489492142 and 35489607971 prove parser success and isolate the post-parse PoC binding misclassification"
    },
    {
      "module": "DEXRuntimeLifecycle",
      "target": "Android Framework Runtime Traversal Dispatch / Surface Draw Consumer Phase 1",
      "reason": "Downstream host-vsync integration, Activity.setContentView(int) layout installation, SurfaceView/SurfaceHolder child-surface callbacks, BitmapFactory.decodeResource with agr_bitmap pixel backing, and Canvas.drawBitmap onto the locked content Surface. agr_dex_game_start_activity still returns with the traversal scheduled and not executed. do_traversal ordering, measure, relayout, layout, root Surface identity, and the first-Surface reschedule are unchanged. The child Surface is created only on the draw-pass pre_draw hook. decodeResource now retains host agr_bitmap pixels keyed by guest Bitmap identity; inJustDecodeBounds still does not decode pixels. const-class and Array.newInstance now preserve the requested array class so check-cast of a multidimensional array succeeds. dx_interpreter.c records a bounded VDISPATCH line for invoke-virtual of start, setRunning, and cleanUp. That record does not select the callee. The vtable repair is in dx_vm.c, outside this stable module. The interpreter now reads the current DxExecutionContext for the frame, exception, and instruction budget, polls a safepoint, and dispatches monitor-enter and monitor-exit. Those edits do not change guest pthread ownership. SurfaceHolder.lockCanvas and unlockCanvasAndPost bind one guest Canvas to the existing content Surface. Canvas.drawBitmap(Bitmap,float,float,null) blits through agr_bitmap_draw into that locked backing. The host mutex is held only around the locked-flag update and uses the existing worker DxExecutionContext. It does not change Thread.start, vtable slots, surfaceCreated, or surfaceChanged. Activity.requestWindowFeature(int) forwards to the Activity-owned Window. The minimum API19 PhoneWindow.requestFeature state is _features, _localFeatures, and _contentParent. FEATURE_NO_TITLE before content sets bit 1<<1, so default features 65 become 67, and returns true. The same Activity-owned Window identity is used by the request and by setContentView. This production change does not modify Activity attach ordering, ViewRoot attach ownership, first traversal, relayout, root Surface identity, Thread.start ownership, or SurfaceHolder lock ownership. It does not redesign the Activity lifecycle. Activity.getIntent() returns the Intent already stored on the Activity _intent field. The launch Intent is attached before onCreate, and the getter returns that same guest object without allocating a replacement. This accessor does not modify Activity attach ordering, ViewRoot attach ownership, first traversal, relayout, root Surface identity, Thread.start ownership, or SurfaceHolder lock ownership. The getIntent witness records the calling DxExecutionContext id. That record does not change the returned Intent. Physical forensic notes after lockCanvas, drawBitmap, and unlockCanvasAndPost publish a fixed sample only when the device recorder is linked. Those notes do not change return values or lock ownership. agr_dex_game_diagnostic_content_lock_busy try-locks an existing content-surface mutex and unlocks it immediately. It does not take a blocking lock, call guest code, draw, or change lock ownership. Surface callback counts increment only when the callback returns without a pending exception. A throw stays pending, matching API19 SurfaceView.updateWindow, and the exception class is copied for the trace. That copy is not guest success. Bitmap.createScaledBitmap and a bounded guest-method window publish forensic samples without changing return values. Root execution-context attribution is diagnostic and does not change Thread.start.",
      "evidence": "API19 ViewRootImpl.scheduleTraversals posts CALLBACK_TRAVERSAL without calling doTraversal, and Choreographer.doCallbacks extracts due callbacks before running them. The Linux host contract keeps the closed explicit first pass at handoff.viewroot_surface_ready with draw_count 0, and setContentView(int) installs one child without consuming the scheduled traversal. Discovery 35668200026 showed the int invoke bound to the View overload. Unchanged Frozen Bubble org.jfedor.frozenbubble_8 sha256 57f4735297befc68c0a7aa6cd9e442ecd250b1b2b38104324a12b6c2d4e18569 showed requestWindowFeature unresolved on execution context 0 before the fix, while the game thread, PenguinSprite.paint, lock 6, post 6, and draw_bitmap 24 still ran. After the fix the same APK records features 65 to 67 on one Window identity that setContentView keeps. The GameThread, Surface, Canvas, and Vector path still reaches GameScreen.paint, elementAt, and PenguinSprite.paint with lock 6, post 6, and draw_bitmap 24. The same APK called FrozenBubble.onCreate pc 11 Activity.getIntent before this accessor existed, and that invoke was unresolved. After the accessor, that invoke returns the Activity _intent object, which is the launch Intent, and the paint path still reaches PenguinSprite.paint with lock 6, post 6, and draw_bitmap 24. The host contract records root execution context 0 and a synthetic worker context's own id on that same witness. Thread.start, run enter, and run exit also publish that execution context when the device recorder is linked. The publish does not change the worker state or the start result. Physical e444be7 at 1080x2340 posted 119 times with draw_bitmap_count 0. created_count and changed_count previously incremented on a -1 throw result. Linux at 1080x2340 with the same APK SHA draws, changes the buffer, and reaches PenguinSprite.paint with Vector size 1."
    },
    {
      "module": "NativeActivityInput",
      "target": "Android Framework Runtime Traversal Dispatch / Surface Draw Consumer Phase 1",
      "reason": "App/main.m snapshot fields report the SurfaceView content Surface. CADisplayLink still calls agr_dex_game_choreographer_frame. The focused first-traversal harness still calls agr_dex_game_do_traversal. NativeActivity input semantics are unchanged. The iphoneos default entry loads unchanged Frozen Bubble, starts the Activity, and drives frames with CADisplayLink. It does not call do_traversal, lockCanvas, drawBitmap, or post.",
      "evidence": "The dispatch discovery path does not call agr_dex_game_do_traversal or agr_guest_start_dex_activity auto-attach. The closed first-traversal probe remains a separate launch. The device validation entry writes Documents/agr-physical-run.json, agr-physical-trace.ndjson, and agr-physical-runtime.json, and keeps harness_called_do_traversal false. A host pthread watchdog records no-progress without calling guest code. The device path registers the content-surface try-lock as that watchdog probe. Finish writes one termination reason into the run file, the finalize trace, and agr-physical-runtime.json."
    }
  ]
}
