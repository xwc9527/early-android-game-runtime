{
  "schema_version": 1,
  "reopens": [
    {
      "module": "NativeActivityInput",
      "target": "DEX Parser Compatibility Phase 1",
      "reason": "App/main.m contains the generic APK smoke harness whose DEX-stage classifier is the evidenced primary blocker; the change does not alter NativeActivity/Input production semantics.",
      "evidence": "focused runs 35489492142 and 35489607971 prove parser success and isolate the post-parse PoC binding misclassification"
    },
    {
      "module": "NativeActivityInput",
      "target": "Android Framework ViewRoot Attach Phase 1",
      "reason": "The ViewRoot attach phase extends the existing Activity/Window handoff boundary and updates shared snapshot reporting; no NativeActivity/Input production semantics are changed.",
      "evidence": "source map framework.viewroot.attach and focused attach contract isolate the change to post-WindowManager.addView ownership."
    },
    {
      "module": "DEXRuntimeLifecycle",
      "target": "Android Framework ViewRoot Attach Phase 1",
      "reason": "Previous stable contract ended at handoff.viewroot_surface; this phase extends the downstream API19 ViewRoot owner from that explicit boundary rather than repairing an unproven regression inside the previous closed contract.",
      "evidence": "Prior Activity/Window closure terminal handoff.viewroot_surface and Android 4.4.4_r2 WindowManagerGlobal.addView -> ViewRootImpl.setView source sequence."
    }
  ]
}
