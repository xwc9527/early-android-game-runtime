{
  "schema_version": 2,
  "classification": "ANDROID_SEMANTIC_BUG",
  "subsystem": "java.lang.Character.forDigit",
  "observed_discontinuity": "Frozen Bubble drawLevelNumber calls Character.forDigit. The game VM registered Ljava/lang/Character; with no methods, so the static invoke returned null instead of a digit character.",
  "upstream": {
    "map_entry": "java.lang.character.for_digit",
    "map_status": "VALID",
    "revision": "Android 4.4.4_r2",
    "source_verified": true,
    "files": [
      "luni/src/main/java/java/lang/Character.java"
    ],
    "call_path": [
      "Character.forDigit"
    ]
  },
  "semantic_class": "PUBLIC_OBSERVABLE",
  "required_semantics": [
    "Radix below MIN_RADIX 2 or above MAX_RADIX 36 returns 0.",
    "A negative digit or a digit greater than or equal to radix returns 0.",
    "A digit below 10 returns the character '0' plus the digit.",
    "A digit of 10 or more returns the character 'a' plus the digit minus 10.",
    "The method is static and returns a char."
  ],
  "agr": {
    "files": [
      "Runtime/DexLoom/VM/dx_vm.c"
    ],
    "call_path": [
      "native_character_fordigit"
    ]
  },
  "differences": [
    {
      "id": "D1",
      "description": "Ljava/lang/Character; had no forDigit method, so invoke-static stored a null result.",
      "relevance": "drawLevelNumber uses forDigit with radix 10 to obtain level digits before BubbleFont.paintChar. A null result is not the character '0' through '9'."
    }
  ],
  "earliest_evidenced_divergence": "API19 Character.forDigit returns a digit character for radix 2..36 and a digit in range. AGR had no method, so the invoke returned null.",
  "causal_status": "REAL_GAME_CONFIRMED",
  "evidence_level": "REAL_GAME_CONFIRMED",
  "evidence": [
    {
      "type": "SOURCE",
      "artifact": "android-4.4.4_r2 libcore luni Character.forDigit returns 0 unless MIN_RADIX <= radix <= MAX_RADIX and 0 <= digit < radix. MIN_RADIX is 2 and MAX_RADIX is 36. A digit below 10 returns (char)(digit + '0'); otherwise (char)(digit + 'a' - 10). Frozen Bubble GameView.drawLevelNumber invokes Character.forDigit with radix 10."
    },
    {
      "type": "CONTRACT",
      "artifact": "Linux character-fordigit-host PASS. forDigit(0,10) is 48, forDigit(9,10) is 57, forDigit(10,10) is 0, forDigit(-1,10) is 0, forDigit(0,2) is 48, forDigit(1,2) is 49, forDigit(10,16) is 97, forDigit(15,16) is 102, forDigit(35,36) is 122, forDigit(36,36) is 0, forDigit(1,1) is 0, forDigit(1,37) is 0, forDigit(0,36) is 48. The registered method is static native shorty CII."
    },
    {
      "type": "REAL_GAME",
      "artifact": "Local unchanged Frozen Bubble APK org.jfedor.frozenbubble_8 sha256 57f4735297befc68c0a7aa6cd9e442ecd250b1b2b38104324a12b6c2d4e18569, Linux host 320x480, diagnostics enabled. After three host frames and 300ms: surfaceCreated 1, surfaceChanged 1, lock 6, post 6, draw_bitmap 18. Six Character.forDigit calls on execution context 1. Each is caller Lorg/jfedor/frozenbubble/GameView$GameThread;.drawLevelNumber pc 18 opcode 0x71 invoke-static method_idx 82 shorty CII, arguments digit 1 radix 10, return tag INT value 49 ('1'). The next guest invoke is BubbleFont.paintChar pc 10 opcode 0x70 invoke-direct method_idx 119 BubbleFont.getCharIndex shorty IC, argument char 49, return 16. The earliest unresolved invoke after that return is Sprite.drawImageClipped pc 1 opcode 0x6e invoke-virtual method_idx 36 Landroid/graphics/Canvas;.save shorty II. This probe is not a new Simulator run."
    }
  ],
  "remaining_uncertainty": [
    "MIN_RADIX and MAX_RADIX fields are not registered. This call site passes the radix as an immediate.",
    "The forDigit probe's next unresolved invoke was Canvas.save. That cluster is framework.graphics.canvas_state_clip. java.util.Vector core is a separate contract.",
    "This confirmation does not change the Simulator content frame on discovery 35711118165."
  ],
  "next_action": "forDigit stays REAL_GAME_CONFIRMED. Canvas save, clipRect, and restore are framework.graphics.canvas_state_clip. Do not rerun the content-frame Simulator for forDigit.",
  "needs_experiment": false,
  "experimental": false,
  "host_adaptation": "native_character_fordigit writes the char into the int result register. It does not allocate a Character object and does not change Canvas, Bitmap, or Thread."
}
